# Secret Files for Deployment

The following local secrets specified in [`../.env.secret`](../.env.secret.tmpl)
should be placed here:

- `.secrets/nginx/combined.htpasswd`
- `.secrets/nginx/reader.htpasswd`
- `.secrets/nginx/editor.htpasswd`

- `.secrets/ssl/cert`
- `.secrets/ssl/private-key.pem`
